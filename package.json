{
  "name": "cafe-data",
  "version": "0.0.2",
  "description": "Data layer for cafe-app",
  "scripts": {
    "build": "bsb -make-world",
    "start": "bsb -make-world -w",
    "clean": "bsb -clean-world",
    "pretest": "npm run build",
    "test": "jest",
    "watch:jest": "jest --watchAll"
  },
  "dependencies": {
    "bs-most": "github:bsommardahl/bs-most",
    "cafe-domain": "github:bsommardahl/cafe-domain",
    "pouchdb": "^6.4.3",
    "pouchdb-find": "^6.4.3"
  },
  "devDependencies": {
    "bs-jest": "^0.3.2",
    "bs-platform": "^4.0.2"
  },
  "jest": {
    "testMatch": [
      "<rootDir>/lib/js/**/*test.{bs.js,js}"
    ]
  }
}
